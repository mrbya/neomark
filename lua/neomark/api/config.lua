local config = {
    disable = {},

    filatypes = { '*.md' },

    keymaps = {
        interactive_mode = '<leader>l',
        forward = '<Right>',
        backward = '<Left>',
        interact = '<CR>'
    },
}

return config
